{
    "version": "2.0.0",
    "tasks": [
        {
        "label": "Run UV Sync",
        "type": "shell",
        "command": "uv sync",
        "options": {
            "cwd": "${workspaceFolder}/api"
        },
        "group": {
            "kind": "build",
            "isDefault": true
        },
        "problemMatcher": []
        }
    ]
}